# Node / Tauri CLI tooling (shell only — the web UI has no build step)
node_modules/

# Cloudflare Workers (wrangler) local cache / state
.wrangler/
# stale PartyKit local state (backend migrated off PartyKit; ignore leftovers)
.partykit/

# Authoritative server's per-room snapshots (runtime state, not source)
server/data/

# Rust / Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# OS
Thumbs.db
.DS_Store

# Crash dumps (e.g. grep.exe.stackdump under MSYS/Git Bash)
*.stackdump
